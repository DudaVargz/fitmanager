{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python create_admin.py && gunicorn meu_projeto.wsgi --log-file -",
    "healthcheckPath": "/login/",
    "restartPolicyType": "ON_FAILURE"
  }
}
